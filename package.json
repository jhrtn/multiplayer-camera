{
  "name": "multiplayer-camera",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8082 --bind 127.0.0.1",
    "build": "node build.mjs",
    "preview": "python3 -m http.server 8083 --bind 127.0.0.1 --directory dist"
  },
  "devDependencies": {
    "pagecrypt": "^7.1.0"
  }
}
